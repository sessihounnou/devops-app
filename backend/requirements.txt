# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.20

# Config
pydantic-settings==2.7.0

# Async HTTP (notifications)
httpx==0.28.1

# Celery / Redis
celery==5.4.0
redis==5.2.1

# Ansible (installé via pip, pas via apt)
# paramiko = SSH pur Python, évite le besoin d'openssh-client système
ansible==10.7.0
ansible-runner==2.4.0
paramiko==3.5.0

# Utilities
python-dotenv==1.0.1
email-validator==2.2.0
